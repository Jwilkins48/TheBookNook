<nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
    <div class="container">
        <a class="navbar-brand text-warning" style="font-size:1.55rem;" href="/">The
            Book
            Nook</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
                @* Change based on if user is signed in *@
                @if (Context.Session.GetInt32("userId") is not null)
                {
                    <li class="nav-item">
                        <a class=" nav-link" href="/books">Book Gallery</a>
                    </li>
                    <div class="d-flex align-items-center text-warning">|</div>
                    <li class="nav-item">
                        <a class="nav-link" href="/books/new">Add Book</a>
                    </li>
                    <div class="d-flex align-items-center text-warning">|</div>
                    <li class="nav-item">
                        <a class=" nav-link" href="/apiBooks">Book of the Day</a>
                    </li>
                    <div class="d-flex align-items-center text-warning">|</div>
                    <li class="nav-item">
                        <a class="nav-link" href="/account/profile">Profile</a>
                    </li>
                    <div class="d-flex align-items-center text-warning">|</div>
                    <li class="nav-item">
                        <a class="nav-link" href="/account/logout">Sign Out</a>
                    </li>

                }
                else
                {
                    <li class="nav-item">
                        <a class="nav-link text-success-emphasis" href="/account/register">Sign Up</a>
                    </li>
                    <div class="d-flex align-items-center text-warning">|</div>
                    <li class="nav-item">
                        <a class="nav-link text-success-emphasis" href="/account/login">Sign In</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>